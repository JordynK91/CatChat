<!-- displays the public-facing profile of a user. if a user clicks on the link to their own profile they are re-directed to the dashboard -->

<div class = 'background'>

<div class = 'jumbotron'>

<h1> Welcome to <%= @user.name %>'s Profile </h1>


<h2> <%= @user.name %>'s Blogs: </h2>

<% @user.blogs.each do |b| %>
<div><%=link_to b.title, blog_path(b.id) %></div>

<% end %>

<h2><%= @user.name %>'s Info:</h2>
<div>My Cat's Name is...<%=@user.catname%></div>
<div>I live in..<%=@user.location%></div>



</div>
</div>